const Background = ({img}) => (
    <img src={img}
         alt="Background"
         className="background"
    style={{
        width: '100%',
        height: 'auto'
    }}/>
)

export default Background;